---
interface Props {
  size?: number;
  class?: string;
  alt?: string;
}
const { size = 32, class: className, alt = 'Montpellier Dépannage' } = Astro.props;
---

<img
  src="/img/logo-mdp.webp"
  alt={alt}
  class:list={['logo-mark', className]}
  loading="eager"
  decoding="async"
  width="600"
  height="333"
  style={`height:${size}px;width:auto;`}
/>

<style>
  .logo-mark {
    flex-shrink: 0;
    display: block;
    object-fit: contain;
  }
</style>
