# Configuration minimale Cloudflare Pages.
# Toute la conf D1 / variables d'env est faite dans le dashboard Pages
# (Settings → Functions → D1 database bindings + Settings → Environment variables).
# Voir CLAUDE.md pour la procédure complète.
#
# Ce fichier sert uniquement au dev local éventuel via `wrangler pages dev`.

name = "mtp-dep"
compatibility_date = "2025-01-15"
pages_build_output_dir = "dist"
